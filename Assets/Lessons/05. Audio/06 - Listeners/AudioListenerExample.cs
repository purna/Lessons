using UnityEngine;

public class AudioListenerExample : MonoBehaviour
{
    /*
    public AudioListener audioListener;

    private void Start()
    {
        // Get the position of the Audio Listener.
        Vector3 listenerPosition = audioListener.transform.position;

        // Play a sound at the position of the Audio Listener.
        AudioSource.PlayClipAtPoint(AudioClip.Create("MySound", 1, 1, 44100, true), listenerPosition);
    }
    */
}